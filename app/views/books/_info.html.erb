<%# ブックマーク数、レビュー数、レビュー平均値を表示する部位を部分テンプレートにまとめて作成 %>

<%
  book = nil if book.nil?
%>

<% if book %>
    ♥ <%= book.bookmarks.length %>   💬 <%= book.reviews.length %>   ★<%= review_average(book.reviews) %>
<% end %>

<%# 上記 review_averageは独自で定めたヘルパーメソッドで、viewをよりシンプルに書くためのモジュール。独自のヘルパーメソッドはapp/helpersの中で定義 %>
